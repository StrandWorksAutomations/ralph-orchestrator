---
name: Claude Agent Initialization — ralph-orchestrator
description: Per-project Claude Code startup file — agent reads on init for project-specific context, auth, and conventions
type: reference
status: implemented
project: ralph-orchestrator
created: 2026-05-18
---

# Claude Agent Initialization — ralph-orchestrator

> **This file ensures proper agent startup and coordination.**
> Template version: 3.3.0 (2026-05-20 — add QA site / web-ask CLI section so agents stop reading "qa page" as a local file)

## MANDATORY Reading Order

**ALL Claude Code agents MUST read these files in this exact order:**

1. **ROOT INSTRUCTIONS** (MANDATORY): `/Users/jonathanbouren/PROJECTS/CLAUDE.md`
2. **MCP CREDENTIALS** (MANDATORY): `/Users/jonathanbouren/PROJECTS/_ops/credentials/.mcp-credentials.json`
3. **PROJECT INSTRUCTIONS**: `./CLAUDE.md` (if exists — overrides/extends root)

---

## Linear Issue Conflict Check

**Before working on any Linear issue:**

1. Check the issue's status in Linear (use `get_issue` MCP tool)
2. If status is `In Progress` or `Done` → DO NOT duplicate the work
3. If status is `Backlog` or `Todo` → Move it to `In Progress` before starting
4. When you complete work on an issue → update its status to `Done`

---

## Git Workflow (mirrors root CLAUDE.md Rule 1)

- **Branch first.** Work on `feature/*`, `fix/*`, `test/*`, or `refactor/*`. Never commit directly to `main` for portfolio code under active human review.
- **Self-merge allowed** to `feature/*` → `main` and `test/*` → parent branch when:
  - All tests pass and the change is reversible (no destructive migrations, no force-push to `main`, nothing that overwrites others' work).
  - The change is mentioned in the next daily brief if it touches a flagship or shipped product (currently: MedCapture, BadgeMedia/Tenetrix Insight).
  - For *major* architectural changes (schema rewrites, dependency upgrades >1 minor version, auth changes, public API breaks): commit on a branch, summarize in the daily brief, and wait for user ack before merging.
- **Never:** force-push `main`, rebase published commits, delete branches the user pushed, skip hooks (`--no-verify`), or amend signed commits.
- **Always:** check `git reflog` is intact before any reset; prefer creating a new commit over amending.

See `/Users/jonathanbouren/PROJECTS/CLAUDE.md` Rule 1 for the authoritative version.

---

## Quick Commands

```bash
# Allocate a port (if running services)
node /Users/jonathanbouren/PROJECTS/port-manager.js allocate "agent-$(date +%s)" 3000 "$(basename $(pwd))"
```

---

## QA Site — `qa.strandautomationworks.com` (NOT a file)

If the user says "post to the qa page", "post on QA", "the Briefs tab", or "post to the QA site" — they mean **https://qa.strandautomationworks.com**, not a file in this project. Do not grep the repo for it.

Tooling (installed at `/Users/jonathanbouren/.local/bin/`):

| Action | CLI |
|--------|-----|
| Ask a decision question | `web-ask --text "..." --title "..." --tier normal\|urgent\|fyi --option LABEL=VALUE` |
| Post an artifact to Briefs tab | `web-brief-post --kind custom --title "..." --file path/to/doc.md` |
| Read user comments back | `web-comments-read` |

See root `CLAUDE.md` Rule 8 for the full routing policy. Never use the in-terminal AskUserQuestion UI on this machine — questions go to the QA site.

---

## Pre-commit Secret Scan (gitleaks)

Active repos run a shared pre-commit gitleaks hook (Linear STR-350) that blocks
commits introducing secrets. On a **fresh clone**, activate it:

```bash
bash /Users/jonathanbouren/PROJECTS/_shared-configs/scripts/install-gitleaks-hook.sh "$PWD"
```

- Hook source of truth: `_shared-configs/hooks/pre-commit-gitleaks`
- Per-clone activation: `git config core.hooksPath .githooks` (the installer sets this)
- Repo-local allowlist: `.gitleaks.toml` (tracked; safe to extend per repo)
- One-off bypass: `SKIP_GITLEAKS=1 git commit ...`
- Permanent disable for this clone: `touch .githooks/pre-commit.disabled`

If the hook is missing in this repo (no `.githooks/pre-commit`), run the installer.

---

## Critical Rules

- **Git Workflow**: Follow root CLAUDE.md Rule 1 — branch conventions + self-merge conditions above. Self-merge is fine on `feature/*` → `main` when conditions are met.
- **Port Management**: Use `port-manager.js` to avoid port conflicts.
- **Linear Conflicts**: Check issue status before starting work — update to Done when complete.
- **File Protection**: Never edit root `CLAUDE.md` without explicit user authorization in the active conversation.
- **AUTONOMOUS TESTING**: NEVER ask the user to test, verify, or run commands. You have Bash, Playwright MCP, Supabase MCP, curl, and test runners. USE THEM. See Root CLAUDE.md Rules 5 & 6.
- **SELF-SUFFICIENCY**: Start dev servers yourself. Install deps yourself. Run builds yourself. The user is NOT your terminal. Only involve them for hardware, auth, purchases, public-facing actions, and design decisions.

---

**Template Version**: 3.3.0
**Last Updated**: 2026-05-20
